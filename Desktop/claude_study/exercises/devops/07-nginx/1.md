# Задание 1: Базовая конфигурация Nginx
**Уровень:** 🟢 Лёгкий
**Тема:** nginx.conf, reverse proxy, proxy_pass
**Ссылка:** notes/devops/07-nginx.md — "Reverse Proxy", "Конфигурация"

---

## Задание

1. Напиши nginx.conf для проксирования на Node.js:
   - listen 80
   - proxy_pass http://localhost:3000
   - Необходимые proxy_set_header

2. Объясни каждую директиву:
   - worker_processes
   - listen
   - server_name
   - proxy_pass
   - proxy_set_header X-Real-IP
   - proxy_set_header Host

3. Добавь раздачу статики:
   - location /_next/static { expires 365d; }
   - location /public { expires 30d; }

4. Добавь gzip сжатие.

5. Объясни: почему Nginx перед Node.js — стандарт на продакшне?
