# Задание 2: React Query и управление состоянием
**Уровень:** 🟡 Средний
**Тема:** React Query, кеширование, мутации
**Ссылка:** notes/devops/02-frontend.md — "React Query"

---

## Задание

1. Настрой React Query в проекте:
   - QueryClientProvider в layout
   - Создай API-клиент (lib/api.ts)

2. Напиши хуки для работы с задачами:
   - `useTasks(boardId)` — загрузка задач
   - `useCreateTask()` — создание с invalidation
   - `useUpdateTask()` — обновление с optimistic update
   - `useDeleteTask()` — удаление

3. Для каждого хука определи:
   - queryKey
   - staleTime
   - Что инвалидировать при мутации

4. Объясни: зачем нужен React Query если можно fetch в useEffect?
   (Кеширование, loading/error состояния, автообновление, дедупликация)
