# Задание 2: Docker Compose
**Уровень:** 🟡 Средний
**Тема:** docker-compose.yml, сервисы, volumes
**Ссылка:** notes/devops/06-docker.md — "Docker Compose"

---

## Задание

1. Напиши docker-compose.yml для TaskBoard:
   - app (Next.js) — build: ., порт 3000, env variables
   - db (PostgreSQL) — image: postgres:16-alpine, volume, healthcheck
   - depends_on с condition: service_healthy

2. Напиши docker-compose.dev.yml (только БД для разработки):
   - PostgreSQL на порту 5432
   - Приложение запускается через npm run dev (без Docker)

3. Напиши команды:
   - Запуск: `docker compose up -d`
   - Логи: `docker compose logs -f app`
   - Миграции: `docker compose exec app npx prisma migrate dev`
   - Остановка: `docker compose down`
   - Полная очистка: `docker compose down -v`

4. Зачем volumes для PostgreSQL? Что будет без них?
