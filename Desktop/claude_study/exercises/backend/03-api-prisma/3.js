// ==================================================
// Задание 3: PUT и DELETE с обработкой ошибок
// Уровень: 🟡 Средний
// Тема: PUT, DELETE, обработка P2025, HTTP-коды
// Ссылка: notes/backend/03-api-prisma.md — "PUT", "DELETE", "Ошибки"
// ==================================================
//
// 1. Напиши PUT /api/users/[id]:
//    - Валидация body через Zod (partial — все поля опциональные)
//    - prisma.user.update({ where: { id }, data })
//    - Обработка P2025 (запись не найдена) → 404
//    - Обработка P2002 (дублирующий email) → 409
//
// 2. Напиши DELETE /api/users/[id]:
//    - prisma.user.delete({ where: { id } })
//    - Обработка P2025 → 404
//    - Успех → 204 (No Content)
//
// 3. Создай универсальный обработчик ошибок Prisma:
//    function handlePrismaError(error: any): NextResponse {
//      if (error.code === "P2002") return ... 409
//      if (error.code === "P2025") return ... 404
//      return ... 500
//    }
//
// 4. Напиши в комментарии все HTTP-коды, которые должен знать:
//    200, 201, 204, 400, 401, 403, 404, 409, 500
//    Для каждого — что означает и когда использовать.
//
// Пиши код ниже:
// ==================================================
