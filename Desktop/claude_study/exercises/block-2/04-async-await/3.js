// ========== Задание async/await #3 — await не блокирует весь JS ==========
// Уровень: 🟢
//
// Определи порядок вывода. Помни: await приостанавливает только
// текущую async-функцию, а не весь JavaScript.
//
// Шаги:
// 1. Прочитай код
// 2. Определи, что выведется мгновенно
// 3. Определи, что выведется через 1 секунду
//
// Тесты:
// Ожидаемый вывод: ???, ???, ???
//
// 📖 Раздел учебника: "await — ожидание результата" → notes/js-async/04-async-await.md
// ================================================

// Пиши код ниже:

// Запиши предполагаемый порядок:
// const ответ = [];

function задержка(мс) {
  return new Promise(resolve => setTimeout(resolve, мс));
}

async function А() {
  console.log("А: начало");
  await задержка(1000);
  console.log("А: конец");
}

А();
console.log("Не ждём А!");
