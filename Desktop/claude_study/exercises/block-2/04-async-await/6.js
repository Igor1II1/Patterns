// ========== Задание async/await #6 — Последовательное vs параллельное ==========
// Уровень: 🟡
//
// Есть две независимые задачи: загрузитьА() и загрузитьБ() (каждая по 1 сек).
//
// Задание:
// 1. Напиши функцию `последовательно()` — выполняет задачи одну за другой (~2 сек)
// 2. Напиши функцию `параллельно()` — выполняет задачи одновременно (~1 сек)
// 3. Замерь время каждой функции через console.time()
//
// Шаги:
// 1. Последовательно: await одну, потом await другую
// 2. Параллельно: запусти обе, потом await обе (или используй Promise.all)
//
// Тесты:
// последовательно() — ~2 секунды
// параллельно()     — ~1 секунда
//
// 📖 Раздел учебника: "Последовательное vs параллельное выполнение" → notes/js-async/04-async-await.md
// ================================================

// Пиши код ниже:

function задержка(мс) {
  return new Promise(resolve => setTimeout(resolve, мс));
}

async function загрузитьА() {
  await задержка(1000);
  return "A";
}

async function загрузитьБ() {
  await задержка(1000);
  return "B";
}

// Напиши обе функции:
