// ========== Задание async/await #7 — await в циклах ==========
// Уровень: 🟡
//
// Есть массив URL: ["/api/1", "/api/2", "/api/3"].
// Напиши две функции:
// 1. `загрузитьПоОчереди(urls)` — загружает ПОСЛЕДОВАТЕЛЬНО через for...of
// 2. `загрузитьВсеСразу(urls)` — загружает ПАРАЛЛЕЛЬНО через map + Promise.all
//
// Шаги:
// 1. Для последовательной: for...of + await
// 2. Для параллельной: urls.map(async ...) + Promise.all
// 3. Замерь время обеих
//
// Тесты:
// загрузитьПоОчереди(["/api/1", "/api/2", "/api/3"]) — ~1.5 сек
// загрузитьВсеСразу(["/api/1", "/api/2", "/api/3"])   — ~0.5 сек
//
// 📖 Раздел учебника: "await в циклах" → notes/js-async/04-async-await.md
// ================================================

// Пиши код ниже:

// Вспомогательная функция (не меняй):
function загрузить(url) {
  return new Promise(resolve => {
    setTimeout(() => resolve("Данные: " + url), 500);
  });
}

// Напиши обе функции:
