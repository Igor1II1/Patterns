// ========== Задание async/await #8 — forEach с async НЕ работает ==========
// Уровень: 🔴
//
// Объясни, почему этот код не работает как ожидается.
// Затем исправь его двумя способами: через for...of и через Promise.all.
//
// Шаги:
// 1. Прочитай "сломанный" код
// 2. Объясни в комментарии, почему массив пуст
// 3. Напиши исправленную версию с for...of
// 4. Напиши исправленную версию с Promise.all + map
//
// 📖 Раздел учебника: "forEach с await — НЕ РАБОТАЕТ" → notes/js-async/04-async-await.md
// ================================================

// Пиши код ниже:

function загрузить(url) {
  return new Promise(resolve => {
    setTimeout(() => resolve("Данные: " + url), 300);
  });
}

// СЛОМАННЫЙ КОД (не меняй, только объясни):
async function сломано() {
  const urls = ["/api/1", "/api/2", "/api/3"];
  const результаты = [];

  urls.forEach(async function(url) {
    const данные = await загрузить(url);
    результаты.push(данные);
  });

  console.log(результаты); // [] — пустой! Почему?
}

// Объясни, почему массив пуст:
// ...

// ИСПРАВЛЕНИЕ 1: через for...of
// async function исправлено1() { ... }

// ИСПРАВЛЕНИЕ 2: через Promise.all + map
// async function исправлено2() { ... }
