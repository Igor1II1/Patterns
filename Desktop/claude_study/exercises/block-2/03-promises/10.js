// ========== Задание Промисы #10 — КОМПЛЕКСНАЯ ЗАДАЧА ==========
// Уровень: 🔴
//
// Перепиши "Конвейер обработки заказа" из задания Колбэки #10,
// но теперь используя ПРОМИСЫ вместо колбэков.
//
// Каждая функция должна ВОЗВРАЩАТЬ промис.
// Используй цепочку .then() вместо вложенных колбэков.
//
// Функции:
// 1. проверитьНаличие(товар) — через 300мс:
//    - Если товар === "нет в наличии" → reject(new Error("Товар отсутствует"))
//    - Иначе → resolve({ товар, статус: "проверен" })
//
// 2. рассчитатьСтоимость(заказ) — через 300мс:
//    - Добавляет цена: 1000 → resolve(заказ)
//
// 3. оформитьДоставку(заказ) — через 300мс:
//    - Добавляет доставка: "курьер" → resolve(заказ)
//
// 4. подтвердитьЗаказ(заказ) — через 300мс:
//    - Добавляет подтверждён: true → resolve(заказ)
//
// Построй цепочку: проверить → рассчитать → оформить → подтвердить
// Добавь .catch() для обработки ошибок и .finally()
//
// Тесты:
// Для "Ноутбук":
// { товар: "Ноутбук", статус: "проверен", цена: 1000, доставка: "курьер", подтверждён: true }
//
// Для "нет в наличии":
// "Ошибка: Товар отсутствует"
// "Обработка завершена" (finally)
//
// 📖 Раздел учебника: все разделы → notes/js-async/03-promises.md
// ================================================

// Пиши код ниже:
