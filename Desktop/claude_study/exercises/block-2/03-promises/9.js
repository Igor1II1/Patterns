// ========== Задание Промисы #9 — Promise.resolve / Promise.reject ==========
// Уровень: 🟡
//
// Напиши функцию `получитьДанные(кэш)`:
// - Если `кэш` не пустой (truthy) → верни Promise.resolve(кэш)
// - Если `кэш` пустой → верни промис, который через 1 сек вернёт "Свежие данные"
//
// Шаги:
// 1. Проверь, есть ли кэш
// 2. Если да — мгновенный промис
// 3. Если нет — промис с задержкой
// 4. Функция ВСЕГДА должна возвращать промис
//
// Тесты:
// получитьДанные("из кэша").then(d => console.log(d)); // "из кэша" (мгновенно)
// получитьДанные(null).then(d => console.log(d));       // "Свежие данные" (через 1 сек)
//
// 📖 Раздел учебника: "Promise.resolve() и Promise.reject()" → notes/js-async/03-promises.md
// ================================================

// Пиши код ниже:
