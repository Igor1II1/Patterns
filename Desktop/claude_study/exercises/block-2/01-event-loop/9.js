// ========== Задание Event Loop #9 — Блокировка потока ==========
// Уровень: 🔴
//
// Этот код демонстрирует блокировку потока.
// 1. Определи, что произойдёт при выполнении
// 2. Объясни, почему setTimeout не сработает вовремя
// 3. Предложи, как это исправить
//
// Шаги:
// 1. Прочитай код
// 2. Подумай, сколько времени займёт тяжёлая операция
// 3. Объясни, почему "таймер" выведется позже чем через 0 мс
//
// 📖 Раздел учебника: "Однопоточность JavaScript", "Зачем нужна асинхронность" → notes/js-async/01-event-loop.md
// ================================================

// Пиши код ниже:

console.log("старт");

setTimeout(() => {
  console.log("таймер (задержка 0)");
}, 0);

// Тяжёлая синхронная операция (имитация)
const начало = Date.now();
while (Date.now() - начало < 3000) {
  // блокируем поток на 3 секунды
}

console.log("после блокировки");

// Вопросы (ответь комментариями):
// 1. Через сколько реально сработает таймер?
// Ответ: ...

// 2. Почему он не сработал через 0 мс как указано?
// Ответ: ...

// 3. Что будет с UI страницы во время работы while?
// Ответ: ...
