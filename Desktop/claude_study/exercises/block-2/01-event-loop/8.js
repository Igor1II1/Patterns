// ========== Задание Event Loop #8 — Сложный микс: синхронный + Promise + setTimeout ==========
// Уровень: 🔴
//
// Определи порядок вывода в этом сложном примере.
// Здесь есть вложенные микрозадачи и макрозадачи.
//
// Шаги:
// 1. Выдели весь синхронный код
// 2. Определи, какие микрозадачи появятся после синхронного кода
// 3. Определи, какие макрозадачи ждут в очереди
// 4. Помни: после КАЖДОЙ макрозадачи — выполнить ВСЕ микрозадачи
//
// Тесты:
// Запиши полный порядок вывода (8 строк)
//
// 📖 Раздел учебника: "Полная картина Event Loop" → notes/js-async/01-event-loop.md
// ================================================

// Пиши код ниже:

// Запиши предполагаемый порядок:
// const ответ = [];

console.log("1");

setTimeout(() => {
  console.log("2");
  Promise.resolve().then(() => console.log("3"));
}, 0);

Promise.resolve().then(() => {
  console.log("4");
  setTimeout(() => console.log("5"), 0);
});

setTimeout(() => {
  console.log("6");
}, 0);

Promise.resolve().then(() => console.log("7"));

console.log("8");
