// ========== Задание Event Loop #10 — КОМПЛЕКСНАЯ ЗАДАЧА ==========
// Уровень: 🔴
//
// Определи полный порядок вывода в этом коде.
// Используй знания всех концепций: стек, Web API, очередь макрозадач,
// очередь микрозадач, приоритеты.
//
// Шаги:
// 1. Пройди код строка за строкой
// 2. Для каждой строки определи: синхронный / микро / макро
// 3. Помни правило: синхронный > микро > макро
// 4. Помни: после каждой макрозадачи проверяются микрозадачи
// 5. Запиши финальный порядок
//
// 📖 Раздел учебника: все разделы → notes/js-async/01-event-loop.md
// ================================================

// Пиши код ниже:

// Запиши предполагаемый порядок:
// const ответ = [];

console.log("A");

setTimeout(() => console.log("B"), 0);

Promise.resolve()
  .then(() => {
    console.log("C");
    return Promise.resolve();
  })
  .then(() => console.log("D"));

setTimeout(() => {
  console.log("E");
  Promise.resolve().then(() => console.log("F"));
}, 0);

Promise.resolve().then(() => console.log("G"));

console.log("H");

// После записи ответа, проверь себя запуском кода.
// Если ошибся — разбери пошагово, где именно ошибка.

// Пошаговый разбор (заполни):
// 1. Синхронный код: ...
// 2. Микрозадачи (первая порция): ...
// 3. Первая макрозадача: ...
// 4. Микрозадачи после неё: ...
// 5. Вторая макрозадача: ...
// 6. Микрозадачи после неё: ...
