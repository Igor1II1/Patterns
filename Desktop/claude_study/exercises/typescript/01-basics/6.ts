// ==================================================
// Задание 6: Сужение типов (Narrowing)
// Уровень: 🟡 Средний
// Тема: typeof, проверка типов в runtime
// Ссылка: notes/typescript/01-basics.md — "Сужение типов"
// ==================================================
//
// 1. Напиши функцию formatValue(value: string | number): string
//    - Если value — строка, верни её в верхнем регистре
//    - Если value — число, верни его с двумя знаками после запятой (toFixed(2))
//    Используй typeof для сужения.
//
// 2. Напиши функцию printLength(value: string | string[]): void
//    - Если строка — выведи "Длина строки: X"
//    - Если массив — выведи "Элементов в массиве: X"
//    Используй Array.isArray() для сужения.
//
// 3. Напиши функцию processInput(input: string | number | boolean): string
//    - string → "Строка: {input}"
//    - number → "Число: {input}"
//    - boolean → "Булевое: {input}"
//    Используй typeof для каждого случая.
//
// 4. Напиши функцию safeParse(value: unknown): string
//    - Если string — верни как есть
//    - Если number — преобразуй в строку через String()
//    - Иначе — верни "Неизвестный тип"
//    Используй typeof для сужения unknown.
//
// Пиши код ниже:
// ==================================================
