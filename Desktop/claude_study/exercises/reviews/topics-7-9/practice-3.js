// ============================================
// Практикум R3 — Темы 7-9 (Объекты, this, Ошибки)
// Задание из 4.12
// ============================================
//
// 📖 Раздел учебника: все разделы главы 04-functions.md
//
// --- Описание ---
// Комплексное задание, объединяющее ВСЕ концепции главы:
// три вида функций, параметры по умолчанию, rest/spread,
// коллбэки, ранний return, чистые функции, замыкание.
//
// --- Что нужно сделать ---
//
// Создай систему обработки списка студентов:
//
// 1. Напиши функцию createStudentProcessor() которая через замыкание
//    хранит список студентов (массив) и возвращает объект с методами:
//
//    - add(name, ...grades) — добавляет студента { name, grades }
//      (grades через rest-параметр), возвращает количество студентов
//
//    - getAverage(name) — средний балл студента (с ранним return если не найден)
//
//    - getAll() — возвращает КОПИЮ списка (чистая функция, без мутации)
//
//    - process(callback) — применяет callback к каждому студенту,
//      возвращает новый массив результатов
//
//    - getTopStudents(minAverage = 70) — возвращает студентов
//      со средним баллом >= minAverage (параметр по умолчанию)
//
// 2. Используй стрелочные функции для коротких коллбэков.
//
// 3. Используй spread для копирования данных.
//
// --- Тесты ---
// const sp = createStudentProcessor();
//
// sp.add("Игорь", 80, 90, 70);       // 1 (количество студентов)
// sp.add("Анна", 95, 88, 92);        // 2
// sp.add("Борис", 60, 55, 70);       // 3
//
// sp.getAverage("Анна")              // 91.67 (примерно)
// sp.getAverage("Неизвестный")       // null (не найден)
//
// sp.getTopStudents()                 // [{ name: "Игорь", ... }, { name: "Анна", ... }]
// sp.getTopStudents(90)               // [{ name: "Анна", ... }]
//
// sp.process(s => s.name)             // ["Игорь", "Анна", "Борис"]
// sp.process(s => ({ name: s.name, avg: средний_балл }))
//
// const all = sp.getAll();
// all.push({ name: "Хакер" });
// sp.getAll().length                  // 3 (оригинал не изменился)
//
// --- Ключевая концепция ---
// Это задание проверяет ВСЕ концепции главы в одной задаче.
// Замыкание для хранения данных, коллбэки для гибкости,
// чистые функции для безопасности, rest/spread для удобства.

// Пиши код ниже:
