// ========== Задание React #6 — useRef vs useState (сравнение) ==========
// Уровень: 🟡
//
// Продемонстрируй разницу между ref и state.
//
// Шаги:
// 1. Плохой счётчик (ref): countRef.current += 1 — НЕ обновляет UI
// 2. Хороший счётчик (state): setCount(prev => prev + 1) — обновляет UI
// 3. Плохой таймер (state): setIntervalId(id) — лишняя перерисовка
// 4. Хороший таймер (ref): intervalRef.current = id — без перерисовки
// 5. Заполни таблицу в комментарии:
//    | Критерий | useState | useRef |
//    | Перерисовка | ??? | ??? |
//    | Мутабельность | ??? | ??? |
//    | Для UI | ??? | ??? |
//
// 📖 Раздел учебника: "useRef vs useState" → notes/react/08-use-ref.md
// ================================================

// Пиши код ниже:
