// ========== Задание React #2 — createContext + Provider + useContext ==========
// Уровень: 🟢
//
// Реши проблему из задания #1 с помощью Context.
//
// Шаги:
// 1. createContext('light') — значение по умолчанию
// 2. <ThemeContext.Provider value={theme}> оборачивает всё дерево
// 3. Button: const theme = useContext(ThemeContext) — получает напрямую!
// 4. Layout, Main, Sidebar НЕ знают про theme — нет props
//
// 📖 Раздел учебника: "Создание контекста" → notes/react/07-use-context.md
// ================================================

// Пиши код ниже:
