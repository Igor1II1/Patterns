// ========== Задание React #4 — useParams: динамические маршруты ==========
// Уровень: 🟡
//
// Создай страницу пользователя с динамическим параметром.
//
// Шаги:
// 1. Определи маршрут: <Route path="/users/:userId" element={<UserProfile />} />
// 2. В компоненте UserProfile получи userId через useParams()
// 3. Помни: useParams всегда возвращает СТРОКИ — преобразуй в число через Number()
// 4. Создай массив users = [{id: 1, name: 'Иван'}, {id: 2, name: 'Мария'}, {id: 3, name: 'Алексей'}]
// 5. Найди пользователя: users.find(u => u.id === Number(userId))
// 6. Если не найден — покажи "Пользователь не найден"
// 7. На главной странице покажи список ссылок: <Link to={`/users/${user.id}`}>{user.name}</Link>
//
// Подсказки:
// - const { userId } = useParams()
// - Значения из URL всегда строки: "/users/42" → userId = "42"
// - Для сравнения с числом нужно преобразование: Number(userId) или parseInt(userId, 10)
//
// 📖 Раздел учебника: "useParams" → notes/react/10-router.md
// ================================================

// Пиши код ниже:
