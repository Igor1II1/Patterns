// ========== Задание Замыкания #2 — Лексическая область видимости ==========
// Уровень: 🟢
//
// БЕЗ запуска кода определи, что выведет каждый console.log.
// Затем запусти и проверь.
//
// Задача — понять цепочку областей видимости (scope chain).
//
const globalVar = "глобальная";

function outer() {
  const outerVar = "внешняя";

  function inner() {
    const innerVar = "внутренняя";
    console.log(innerVar);  // ?
    console.log(outerVar);  // ?
    console.log(globalVar); // ?
  }

  inner();
  // console.log(innerVar); // Что произойдёт? Раскомментируй и проверь.
}

outer();

// Ответь на вопросы (в комментариях):
// 1. Почему inner() видит outerVar?
// 2. Почему outer() НЕ видит innerVar?
// 3. В каком порядке JS ищет переменные?

// 📖 Раздел учебника: "Лексическая область видимости" → notes/js-advanced/02-closures.md
// ================================================

// Пиши ответы ниже:
