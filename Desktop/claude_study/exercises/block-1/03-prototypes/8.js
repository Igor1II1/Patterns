// ========== Задание Прототипы #8 — Переопределение методов прототипа ==========
// Уровень: 🟡
//
// 1. Создай функцию-конструктор `Animal(name)`.
// 2. Добавь в Animal.prototype метод `speak()`, возвращающий `"[name] издаёт звук"`.
// 3. Создай два экземпляра: `cat` и `dog`.
// 4. Переопредели `speak` ТОЛЬКО для dog (собственный метод): `"[name] лает"`.
//
// 5. Проверь: cat использует метод из прототипа, dog — свой собственный.
// 6. Вызови метод прототипа для dog явно через Animal.prototype.speak.call(dog).
//
// Тесты:
// console.log(cat.speak()); // "[name] издаёт звук" — из прототипа
// console.log(dog.speak()); // "[name] лает" — собственный (override)
//
// // Вызов метода прототипа напрямую:
// console.log(Animal.prototype.speak.call(dog)); // "[name] издаёт звук"
//
// 📖 Раздел учебника: "Переопределение методов прототипа" → notes/js-advanced/03-prototypes.md
// ================================================

// Пиши код ниже:
