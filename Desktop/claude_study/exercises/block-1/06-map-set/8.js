// ========== Задание Map и Set #8 — WeakMap (кэш) ==========
// Уровень: 🟡
//
// 1. Создай WeakMap `cache`.
//
// 2. Напиши функцию `heavyCalc(obj)`:
//    - Если результат для этого объекта уже есть в cache — вернуть из кэша
//    - Иначе вычислить (для примера: Object.keys(obj).length * 100), сохранить в cache и вернуть
//
// 3. Продемонстрируй, что:
//    - Второй вызов с тем же объектом берёт из кэша
//    - Разные объекты имеют разные записи в кэше
//
// Тесты:
// const obj1 = { a: 1, b: 2, c: 3 };
// const obj2 = { x: 1 };
//
// console.log(heavyCalc(obj1)); // 300 (вычислено)
// console.log(heavyCalc(obj1)); // 300 (из кэша — без повторного вычисления)
// console.log(heavyCalc(obj2)); // 100 (вычислено для другого объекта)
//
// // WeakMap нельзя итерировать:
// // for (const [k, v] of cache) {} — TypeError
//
// 📖 Раздел учебника: "WeakMap" → notes/js-advanced/06-map-set.md
// ================================================

// Пиши код ниже:
