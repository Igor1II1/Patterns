// ========== Задание Итераторы #10 — КОМПЛЕКСНАЯ ЗАДАЧА: Утилиты для генераторов ==========
// Уровень: 🔴
//
// Создай набор утилитарных генераторов, работающих с любым итерируемым:
//
// 1. `take(iterable, n)` — первые n элементов
// 2. `skip(iterable, n)` — пропустить первые n, выдать остальные
// 3. `filter(iterable, predicate)` — только элементы, для которых predicate(x) === true
// 4. `map(iterable, transform)` — преобразовать каждый элемент
// 5. `zip(iterableA, iterableB)` — объединить попарно: [a1, b1], [a2, b2], ...
//    (остановиться, когда один из итерируемых закончится)
// 6. `chain(...iterables)` — объединить несколько итерируемых последовательно
//
// Все функции — генераторы. Они должны быть ленивыми (вычислять по требованию).
//
// Тесты:
// function* naturals() { let n = 1; while (true) yield n++; }
//
// console.log([...take(naturals(), 5)]); // [1, 2, 3, 4, 5]
// console.log([...skip(naturals(), 3)]  // бесконечно! Используй с take:
// console.log([...take(skip(naturals(), 3), 5)]); // [4, 5, 6, 7, 8]
//
// console.log([...take(filter(naturals(), n => n % 2 === 0), 5)]);
// // [2, 4, 6, 8, 10]
//
// console.log([...take(map(naturals(), n => n * n), 5)]);
// // [1, 4, 9, 16, 25]
//
// console.log([...zip([1, 2, 3], ["a", "b", "c"])]);
// // [[1, "a"], [2, "b"], [3, "c"]]
//
// console.log([...chain([1, 2], [3, 4], [5, 6])]);
// // [1, 2, 3, 4, 5, 6]
//
// // Композиция (ленивая цепочка):
// const result = take(
//   map(
//     filter(naturals(), n => n % 3 === 0),
//     n => n * 10
//   ),
//   4
// );
// console.log([...result]); // [30, 60, 90, 120]
//
// 📖 Раздел учебника: ВСЕ разделы → notes/js-advanced/09-iterators.md
// ================================================

// Пиши код ниже:
