// ========== Задание Продвинутые функции #2 — Мемоизация ==========
// Уровень: 🟡
//
// Мемоизация — кэширование результатов функции, чтобы не вычислять одно и то же дважды.
//
// Напиши функцию `memoize(fn)`, которая:
// - Возвращает новую функцию
// - При первом вызове с аргументом — вызывает fn и сохраняет результат
// - При повторном вызове с тем же аргументом — возвращает из кэша (без вызова fn)
// - Используй Map для кэша (ключ — аргумент, значение — результат)
//
// Тесты:
// let callCount = 0;
// function slowSquare(n) {
//   callCount++;
//   console.log(`Вычисляю ${n}^2...`);
//   return n * n;
// }
//
// const fastSquare = memoize(slowSquare);
//
// console.log(fastSquare(5)); // "Вычисляю 5^2..." → 25
// console.log(fastSquare(5)); // 25 (без "Вычисляю..." — из кэша!)
// console.log(fastSquare(3)); // "Вычисляю 3^2..." → 9
// console.log(fastSquare(3)); // 9 (из кэша)
// console.log(callCount);     // 2 (fn вызвана только 2 раза, не 4)
//
// 📖 Раздел учебника: "Мемоизация" → notes/js-advanced/10-advanced-functions.md
// ================================================

// Пиши код ниже:
