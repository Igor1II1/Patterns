// ============================================
// Задание 6.7 — flat, reverse, Array.from
// Уровень: 🟡 Средний
// ============================================
//
// 📖 Раздел учебника: "flat()", "reverse()", "Array.from()" (06-arrays.md)
//
// --- Описание ---
// flat(depth) — разворачивает вложенные массивы.
// reverse() — переворачивает массив (МУТИРУЕТ!).
// Array.from — создаёт массив из "массивоподобного" объекта.
//
// --- Что нужно сделать ---
//
// 1. flat:
//    const nested = [1, [2, 3], [4, [5, [6]]]];
//    a) Разверни на 1 уровень
//    b) Разверни полностью (flat(Infinity))
//
// 2. reverse:
//    const arr = [1, 2, 3, 4, 5];
//    a) Переверни массив БЕЗ мутации оригинала (через [...arr].reverse())
//    b) Покажи что оригинал не изменился
//
// 3. Array.from:
//    a) Создай массив из строки: Array.from("Hello") → ["H", "e", "l", "l", "o"]
//    b) Создай массив чисел от 1 до 10:
//       Array.from({ length: 10 }, (_, i) => i + 1)
//       ⚠️ МИНИ-СПРАВКА: { length: 10 } — это объект с одним свойством length.
//       Array.from использует его чтобы знать сколько элементов создать.
//       Объекты подробно — в теме 07-objects.
//       _ — соглашение: "этот параметр не нужен, но обязателен по синтаксису".
//    c) Создай массив квадратов от 1 до 5:
//       Array.from({ length: 5 }, (_, i) => (i + 1) ** 2)
//
// 4. Напиши функцию range(start, end, step = 1):
//    - Возвращает массив чисел от start до end (включительно)
//    - С шагом step
//    Используй Array.from.
//
// --- Тесты ---
// flat(1): [1, 2, 3, 4, [5, [6]]]
// flat(Infinity): [1, 2, 3, 4, 5, 6]
//
// range(1, 5)     → [1, 2, 3, 4, 5]
// range(0, 10, 2) → [0, 2, 4, 6, 8, 10]
// range(5, 1, -1) → [5, 4, 3, 2, 1]
//
// --- Ключевая концепция ---
// flat(Infinity) — разворачивает любую вложенность.
// reverse() МУТИРУЕТ — всегда делай копию.
// Array.from — мощный генератор массивов.
