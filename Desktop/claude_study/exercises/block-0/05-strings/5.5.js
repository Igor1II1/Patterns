// ============================================
// Задание 5.5 — split и join
// Уровень: 🟡 Средний
// ============================================
//
// 📖 Раздел учебника: "split() / join()" (05-strings.md)
//
// ⚠️ МИНИ-СПРАВКА: Массивы (Array)
// Массивы подробно изучаются в теме 06-arrays. Здесь достаточно знать:
//   - Массив — упорядоченный список значений: ["a", "b", "c"]
//   - split(",") — строковый метод, возвращает МАССИВ частей строки
//   - join(",") — метод массива, собирает массив обратно в СТРОКУ
//   - reverse() — метод массива, переворачивает порядок элементов
// split/join — "мост" между строками и массивами, поэтому изучаются в теме строк.
//
// --- Описание ---
// split(separator) — разбивает строку в массив.
// join(separator) — собирает массив в строку.
// Это обратные операции: str.split(",").join(",") === str.
//
// --- Что нужно сделать ---
//
// 1. Напиши функцию reverseWords(str):
//    - Переворачивает порядок слов в строке
//    - "Привет мир JavaScript" → "JavaScript мир Привет"
//    Используй split + reverse + join.
//
// 2. Напиши функцию toCamelCase(str):
//    - Превращает строку с дефисами или пробелами в camelCase
//    - "hello-world" → "helloWorld"
//    - "my variable name" → "myVariableName"
//    Используй split, затем первое слово маленькими буквами,
//    остальные — с заглавной первой.
//
// --- Тесты ---
// reverseWords("Привет мир")           → "мир Привет"
// reverseWords("a b c d")              → "d c b a"
//
// toCamelCase("hello-world")           → "helloWorld"
// toCamelCase("my variable name")      → "myVariableName"
// toCamelCase("border-top-color")      → "borderTopColor"
//
// --- Ключевая концепция ---
// split/join — переход между строкой и массивом (и обратно).
// Паттерн: str → split → обработка массива → join → str.
