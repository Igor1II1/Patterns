// ========== Задание 2.3 ==========
// Перепиши с if/else на тернарный оператор и обратно.
//
// Раздел учебника: "Тернарный оператор"

// --- Часть 1: перепиши через тернарный оператор ---

function isOnline(status) {
  let result = status ? "в сети" : "не в сети";
  return result;
}

console.log(isOnline(true));
console.log(isOnline(false));

// if (isOnline) {
//   status = "в сети";
// } else {
//   status = "не в сети";
// }

// --- Часть 2: перепиши через if/else ---
// const label = score >= 60 ? "Сдал" : "Не сдал";

function label(score) {
  if (score >= 60) {
    return "сдал";
  } else {
    return "не сдал";
  }
}

console.log(label(20));
console.log(label(80));

// ========== Разбор задания ==========
// Что делали:    переводили код между if/else и тернарным оператором.
// Почему так:    тернарный оператор — это выражение (возвращает значение),
//                if/else — это инструкция (выполняет действия).
//                Тернарный: const x = условие ? значениеА : значениеБ
//                if/else:   if (условие) { ... } else { ... }
// Для чего:      уметь выбирать подходящий инструмент: тернарный для коротких присваиваний,
//                if/else для сложной логики с несколькими действиями.
// Главное понять: тернарный = "выбрать одно из двух значений". Не вкладывай тернарные
//                друг в друга — это нечитаемо. Для сложной логики используй if/else.
