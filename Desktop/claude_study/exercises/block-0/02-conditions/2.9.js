// ============================================
// Задание 2.9 — Короткое замыкание (short-circuit)
// Уровень: 🟡 Средний
// ============================================
//
// 📖 Раздел учебника: "Логические операторы", "Short-circuit evaluation" (02-conditions.md)
//
// --- Описание ---
// && возвращает ПЕРВОЕ falsy значение (или последнее, если все truthy).
// || возвращает ПЕРВОЕ truthy значение (или последнее, если все falsy).
// Это можно использовать для условного выполнения кода.
//
// --- Что нужно сделать ---
//
// Шаг A — В ЧАТ (до запуска):
// Что вернёт каждое выражение? Объясни по шагам.
//
// 1. "hello" && 42
// 2. 0 && "world"
// 3. "" || "default"
// 4. "value" || "default"
// 5. null && console.log("выведется?")
// 6. true && console.log("а это?")
// 7. false || false || "last"
// 8. 1 && 2 && 3
// 9. 1 && 0 && 3
//
// Шаг B — В ФАЙЛ:
// 1. Напиши функцию greetUser(user) которая использует && и || для:
//    - Если user существует И у него есть name → вернуть "Привет, {name}!"
//    - Иначе → "Привет, гость!"
//    Сделай это ОДНОЙ строкой через && и ||, без if.
//
// 2. Напиши функцию getFirst(...values) которая возвращает первое
//    truthy значение из переданных аргументов (используй || цепочкой
//    или цикл).
//
// --- Тесты ---
// greetUser({ name: "Игорь" })  → "Привет, Игорь!"
// greetUser({ name: "" })       → "Привет, гость!"  (пустая строка — falsy)
// greetUser(null)               → "Привет, гость!"
// greetUser({})                 → "Привет, гость!"
//
// getFirst(null, 0, "", "значение", 42) → "значение"
// getFirst(null, undefined, 0)          → 0 (все falsy, вернуть последнее)
//
// --- Ключевая концепция ---
// && — "выполни правое, только если левое truthy"
// || — "выполни правое, только если левое falsy"
