// ============================================
// Задание 2.8 — Выражение vs инструкция
// Уровень: 🟢 Базовый
// ============================================
//
// 📖 Раздел учебника: "Выражение vs инструкция" (02-conditions.md)
//
// --- Описание ---
// Выражение (expression) — возвращает значение (5 + 3, x > 0 ? "да" : "нет").
// Инструкция (statement) — выполняет действие, НЕ возвращает значение (if, for, while).
// Тернарный оператор — это ВЫРАЖЕНИЕ, if/else — это ИНСТРУКЦИЯ.
//
// --- Что нужно сделать ---
//
// Шаг A — В ЧАТ:
// Для каждой строки скажи: это выражение или инструкция? Почему?
//
// 1. 5 + 3
// 2. if (true) { }
// 3. x > 0 ? "да" : "нет"
// 4. let y = 10;
// 5. "hello".toUpperCase()
// 6. switch (x) { case 1: break; }
// 7. console.log("тест")
// 8. x = 5
//
// Шаг B — В ФАЙЛ:
// 1. Напиши функцию getLabel(status) двумя способами:
//    - Способ 1: через if/else (инструкция)
//    - Способ 2: через тернарный оператор (выражение)
//    status: "active" → "Активен", "blocked" → "Заблокирован", иначе → "Неизвестно"
//
// 2. Покажи где тернарный можно использовать, а if/else — нельзя:
//    const message = ???;  // подставь тернарный — работает
//    const message2 = ???; // подставь if/else — НЕ работает (объясни почему)
//
// --- Тесты ---
// getLabel("active")  → "Активен"
// getLabel("blocked") → "Заблокирован"
// getLabel("other")   → "Неизвестно"
//
// --- Ключевая концепция ---
// Выражение можно подставить в const x = ???, инструкцию — нельзя.
