// ============================================
// Задание 8.2 — Потеря this в объектах и 3 способа исправить
// Уровень: 🟡 Средний
// ============================================
//
// 📖 Раздел учебника: "Типичная ловушка", "Случай 3" (08-this.md)
//
// --- Описание ---
// Когда метод объекта присваивается в переменную — this теряется.
// Функция вызывается как standalone, а не через точку.
// Есть 3 способа это исправить: bind, стрелочная функция, const self = this.
//
// --- Что нужно сделать ---
//
// Шаг A — В ЧАТ (до запуска):
// Что выведет каждый console.log? Объясни почему.
//
// const user = {
//   name: "Игорь",
//   greet() {
//     console.log("Привет, " + this.name);
//   }
// };
//
// user.greet();      // ?
// const fn = user.greet;
// fn();              // ?
//
// Шаг B — В ФАЙЛ:
// 1. Скопируй код выше, запусти, добавь комментарии с объяснением
//
// 2. Исправь потерю this ТРЕМЯ способами:
//
//    Способ 1 — bind:
//    Создай const greetBound = user.greet.bind(user);
//    Вызови greetBound(); // должно вывести "Привет, Игорь"
//
//    Способ 2 — стрелочная обёртка:
//    Создай const greetArrow = () => user.greet();
//    Вызови greetArrow(); // должно вывести "Привет, Игорь"
//
//    Способ 3 — сохранение в переменную (const self = this):
//    Создай объект user2:
//    const user2 = {
//      name: "Анна",
//      getGreeter() {
//        const self = this;
//        return function() {
//          console.log("Привет, " + self.name);
//        };
//      }
//    };
//    const greeter = user2.getGreeter();
//    greeter(); // должно вывести "Привет, Анна"
//
// 3. Для каждого способа напиши комментарий:
//    - ПОЧЕМУ это работает (технически)
//    - В каких ситуациях этот способ удобнее
//
// --- Тесты ---
// user.greet()       → "Привет, Игорь" (вызов через точку, this = user)
// fn()               → "Привет, undefined" (standalone, this потерян)
// greetBound()       → "Привет, Игорь" (bind привязал this навсегда)
// greetArrow()       → "Привет, Игорь" (стрелочная вызывает через точку)
// greeter()          → "Привет, Анна" (self сохранил ссылку на this)
//
// --- Ключевая концепция ---
// Потеря this — самая частая ошибка начинающих.
// Запомни: если метод "оторван" от объекта (присвоен в переменную,
// передан как коллбэк) — this теряется. Исправляй одним из 3 способов.
