// ========== Задание 1.12 ==========
// Разберись в копировании по значению и по ссылке.
// Шаг A — в чат: для каждого блока кода скажи что выведет последний console.log.
//   Объясни почему — по объекту это ссылка или копия?
// Шаг B — в файл:
//   1. Скопируй три блока кода и к каждой строке добавь комментарий —
//      что происходит с памятью: создаётся копия или передаётся ссылка?
//   2. Напиши три способа сделать независимую копию объекта.
//      После каждого — измени копию и выведи оригинал (он не должен измениться).
// Раздел учебника: "Копирование по значению vs по ссылке"

//передается копия ссылки
let a = { count: 0 };// создается объект в памяти 
let b = a; // b получает копию ссылки объекта a
b.count = 5; // меняем объект через и - a тоже видит это изменение и меняет его 
console.log(a.count);  // 
//передается копия ссылки
let x = [1, 2, 3]; // создается объект
let y = x;  // происходит копия по ссылке
y.push(4); // теперь y добавлеям 4 в конец массива x  - это тоже видит изменение и применяет
console.log(x);  // 1,2,3,4
// передается копия значения
let p = 10; // создается примитив
let q = p;// q получает копию значения  p 
q = 20; // меняем значение примитива
console.log(p);  // 10

const original = {name:"Igor", age:"27"};

const copy1 ={...original};
copy1.name="Петр";
console.log(original.name)
console.log(copy1)

const copy2 = Object.assign({}, original);
copy2.age=28;
console.log(original.age)

const deepCopy = structuredClone(original);
deepCopy.name="Костя";
console.log(original.name);

// ========== Разбор задания ==========
// Что делали:    разобрали разницу между копированием по значению (примитивы)
//               и по ссылке (объекты). Применили 3 способа независимого копирования.
// Почему так:    примитивы (number, string, boolean...) при присваивании копируются —
//               каждая переменная хранит своё значение. Объекты и массивы — нет:
//               let b = a копирует ссылку (адрес), обе переменные указывают на один объект.
// Для чего:      если не понимать разницу — легко случайно изменить данные через "копию",
//               которая на самом деле указывает на тот же объект.
// Главное понять:
//   let b = a;  (объект)    → копия ССЫЛКИ, один объект на двоих
//   let q = p;  (примитив)  → копия ЗНАЧЕНИЯ, каждый сам по себе
//   { ...obj }  / Object.assign({}, obj) → поверхностная независимая копия
//   structuredClone(obj)                 → глубокая независимая копия
