// ========== Задание 1.2 ==========
// Что выведет код? Ответь до запуска, потом проверь в консоли.
// Для каждой строки объясни почему именно такой результат.
//
// Раздел учебника: "Оператор typeof", "Примитивные типы", "Неявное преобразование типов"

// object — историческая ошибка, хотя null не object
console.log(typeof null);
// object — массивы в JS это объекты
console.log(typeof []);
// function
console.log(typeof function(){});
// undefined
console.log(typeof undefined);
// number — NaN это "не число", но тип у него number
console.log(typeof NaN);

const a = "5";
const b = 2;
// "52" — потому что JS преобразует оба значения в строку,
// это работает только если оператор сложения
console.log(a + b);   // почему не 7?
// 3 — преобразует все в число и производит вычитание
console.log(a - b);   // почему не ошибка?
// 10 — преобразует в числа и умножает
console.log(a * b);   // почему число?

// ========== Разбор задания ==========
// Что делали:    предсказывали результаты typeof и операций со смешанными типами.
// Почему так:    + со строкой делает конкатенацию, а - * / конвертируют в число.
//                typeof врёт для null ("object") и NaN ("number" — NaN это значение типа number).
// Для чего:      понимание неявных преобразований объясняет откуда берутся "странные" баги.
// Главное понять: + уникален — единственный оператор который делает конкатенацию.
//                - * / всегда пытаются сделать числа.
//                typeof null → "object" (историческая ошибка, null не объект).
//                typeof NaN  → "number" (NaN — значение типа number, не отдельный тип).
