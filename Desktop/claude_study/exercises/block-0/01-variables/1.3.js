// ========== Задание 1.3 ==========
// Что выведет код? Ответь до запуска. После — объясни механизм hoisting для каждого случая.
// ВНИМАНИЕ: этот файл намеренно содержит ошибки — это часть задания.
// Запускай по одному блоку (комментируй остальные), чтобы увидеть каждую ошибку отдельно.
//
// Вопросы:
// 1. Почему var x не вызывает ошибку, а let y вызывает?
// 2. Почему sayHi() работает до объявления, а sayBye() — нет?
//
// Раздел учебника: "var: проблема 2 — Hoisting", "let: Hoisting с TDZ"

// undefined — JS прочитает код дважды и выполнит hoisting (поднятие) для var,
// поэтому undefined: переменная объявлена, но значение ещё не задано
console.log(x);
// ReferenceError — temporal dead zone: у let hoisting так не работает как с var
console.log(y);
var x = 10;
let y = 20;

// выведет "Привет" — потому что мы объявляем функцию,
// JS может поднять её вместе с телом
sayHi();
// ReferenceError — expression-функция работает как let: TDZ
sayBye();

function sayHi() { console.log("Привет"); }
const sayBye = function() { console.log("Пока"); };

// ========== Разбор задания ==========
// Что делали:    изучали hoisting — как JS "читает" код дважды перед выполнением.
// Почему так:    JS сначала проходит весь файл и поднимает объявления вверх.
//                var → поднимается с undefined (нет ошибки, но значения ещё нет).
//                let/const → поднимаются без значения — TDZ, обращение до объявления = ошибка.
//                function declaration → поднимается целиком с телом, можно вызвать до объявления.
//                function expression (const f = function(){}) → работает как let/const — TDZ.
// Для чего:      понять почему функции можно вызывать до их объявления,
//                и почему var — источник скрытых багов.
// Главное понять: var скрывает ошибки (даёт undefined вместо ошибки) → скрытые баги.
//                let/const явно говорят об ошибке → лучше.
//                Явная ошибка лучше скрытого бага. Поэтому var не используют.
