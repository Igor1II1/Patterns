// ============================================
// Задание 7.2 — Optional chaining и методы объекта
// Уровень: 🟢 Базовый
// ============================================
//
// 📖 Раздел учебника: "Optional Chaining (?.)","Методы" (07-objects.md)
//
// --- Описание ---
// ?. — безопасный доступ к свойствам: если слева null/undefined,
// возвращает undefined вместо ошибки.
// Метод объекта — функция, записанная как свойство.
//
// --- Что нужно сделать ---
//
// ЧАСТЬ A — Optional chaining:
// 1. Шаг В ЧАТ: что выведет каждый console.log?
//
// const user = { name: "Игорь", address: { city: "Москва" } };
// console.log(user.address.city);       // ?
// console.log(user.phone?.number);      // ?
// console.log(user.address?.zip);       // ?
//
// const user2 = null;
// // console.log(user2.name);           // ? (без ?.)
// console.log(user2?.name);             // ? (с ?.)
//
// 2. Напиши функцию getCity(user):
//    - Безопасно возвращает город пользователя
//    - Если user, address или city не существует — "Неизвестно"
//    - Используй ?. и ??
//
// ЧАСТЬ B — Методы:
// 3. Создай объект calculator с методами:
//    - add(a, b) — сложение
//    - subtract(a, b) — вычитание
//    - multiply(a, b) — умножение
//    Используй сокращённый синтаксис методов: { add(a, b) { ... } }
//
// --- Тесты ---
// getCity({ address: { city: "Москва" } }) → "Москва"
// getCity({ address: {} })                  → "Неизвестно"
// getCity(null)                             → "Неизвестно"
// getCity({})                               → "Неизвестно"
//
// calculator.add(2, 3)      → 5
// calculator.multiply(4, 5) → 20
//
// --- Ключевая концепция ---
// ?. — "если есть, обратись; если нет — undefined (без ошибки)".
// ?. + ?? = безопасный доступ с дефолтным значением.
