-- ==================================================
-- Задание 3: Агрегатные функции
-- Уровень: 🟡 Средний
-- Тема: COUNT, SUM, AVG, MIN, MAX
-- Ссылка: notes/sql/02-join-group.md — "Агрегатные функции"
-- ==================================================
--
-- 1. Посчитай общее количество пользователей:
--    SELECT COUNT(*) FROM users;
--
-- 2. Посчитай количество активных пользователей:
--    SELECT COUNT(*) FROM users WHERE is_active = true;
--
-- 3. Найди среднюю цену товаров:
--    SELECT AVG(price) FROM products;
--
-- 4. Найди самый дорогой и самый дешёвый товар:
--    SELECT MAX(price) AS max_price, MIN(price) AS min_price FROM products;
--
-- 5. Посчитай общую сумму всех заказов:
--    SELECT SUM(total_price) FROM orders;
--
-- 6. Посчитай среднюю сумму оплаченных заказов:
--    SELECT AVG(total_price) FROM orders WHERE status = 'paid';
--
-- 7. Посчитай количество товаров в наличии и не в наличии:
--    Два отдельных запроса с WHERE.
--
-- Пиши SQL ниже:
-- ==================================================
