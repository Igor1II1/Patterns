-- ==================================================
-- Задание 3: SELECT — базовые запросы
-- Уровень: 🟢 Лёгкий
-- Тема: SELECT, WHERE, ORDER BY, LIMIT, OFFSET
-- Ссылка: notes/sql/01-select-insert.md — "SELECT"
-- ==================================================
--
-- 1. Выбери всех пользователей:
--    SELECT * FROM users;
--
-- 2. Выбери только имена и email:
--    SELECT name, email FROM users;
--
-- 3. Выбери пользователей старше 25:
--    WHERE age > 25
--
-- 4. Выбери активных пользователей с email на @mail.ru:
--    WHERE is_active = true AND email LIKE '%@mail.ru'
--
-- 5. Отсортируй пользователей по возрасту (от старшего к младшему):
--    ORDER BY age DESC
--
-- 6. Выбери 3 самых молодых пользователя:
--    ORDER BY age ASC LIMIT 3
--
-- 7. Пагинация: вторая страница по 2 записи:
--    LIMIT 2 OFFSET 2
--
-- 8. Выбери товары дороже 1000 и в наличии, отсортируй по цене:
--    WHERE price > 1000 AND in_stock = true ORDER BY price ASC
--
-- Пиши SQL ниже:
-- ==================================================
